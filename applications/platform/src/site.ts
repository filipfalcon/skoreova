/**
 * The origin every canonical URL is built on. The `.cz` alias serves the same Worker and points
 * here, which is what makes one origin the right answer rather than the requested host.
 */
export const SITE_ORIGIN = 'https://platform.skoreova.com';
